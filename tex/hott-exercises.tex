\documentclass[fleqn]{article}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[color]{coqdoc}
\usepackage{enumitem}
\usepackage[margin=1in]{geometry}
\usepackage{mathtools}
\usepackage{noweb}
\usepackage{python}
\usepackage{tocloft}
\usepackage{xspace}
\usepackage[all]{xy}

\usepackage{color}

\definecolor{ltblue}{rgb}{0,0.4,0.4}
\definecolor{dkblue}{rgb}{0,0.05,0.4}
\definecolor{dkgreen}{rgb}{0,0.35,0}
\definecolor{dkviolet}{rgb}{0.3,0,0.5}
\definecolor{dkred}{rgb}{0.5,0,0}
\definecolor{grey}{rgb}{0.5,0.5,0.6}

% Formatting
\newcommand{\exer}[2]{\paragraph{${}^{*}$Exercise #1 (p.~#2)}}
\newcommand{\exerdone}[2]{\paragraph{Exercise #1 (p.~#2)}}
\newcommand{\soln}{\paragraph*{Solution}}
\setcounter{tocdepth}{5}
\cftsetindents{paragraph}{0pt}{0pt}

% Typeface
\usepackage{tgpagella}
\usepackage[T1]{fontenc}
\usepackage{mathpazo}
\linespread{1.05}

\definecolor{my-blue}{RGB}{1,49,94}
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  citecolor=my-blue,
  linkcolor=my-blue,
  pdfinfo={
    Title={},
    Author={John Dougherty},
  }
}

\newcommand{\term}{\textbf}

\input{macros}


\title{Exercises from the \emph{HoTT Book}}
\author{John Dougherty}
\begin{document}


\maketitle
\thispagestyle{empty}

\begin{python}
import glob
for chap in glob.iglob('chap*.tex'):
    print r'\input{{{chap}}}'.format(chap=chap)
\end{python}

%\appendix
%\section{Statistics}
%
%\begin{python}
%from __future__ import division
%import glob
%import subprocess
%
%total_done = 0
%total_exer = 0
%
%print r'\begin{itemize}'
%for chap in glob.iglob('chap*.tex'):
%    this_done = 0
%    this_total = 0
%
%    try:
%        this_done = int(subprocess.check_output(['grep', '-c', r'%DONE', chap]))
%    except subprocess.CalledProcessError:
%        this_done = 0
%
%    try:
%        this_total = int(subprocess.check_output(['grep', '-c', r'\\exer', chap]))
%    except subprocess.CalledProcessError:
%        this_total = 0
%
%    try:
%        print r'\item {chap}: {done}/{total} {ratio:.2f}\%'.format(chap=chap,
%          done=this_done, total=this_total, ratio=((this_done/this_total)*100))
%    except ZeroDivisionError:
%        print r'\item {chap}: 0/0 100.00\%'.format(chap=chap)
%
%    total_done += this_done
%    total_exer += this_total
%
%print r'\end{itemize}'
%
%try:
%  print '{done}/{total}: {ratio:.2f}\%'.format(done=total_done,
%                              total=total_exer,
%                              ratio=((total_done/total_exer)*100))
%  if total_done/total_exer >= .25:
%    print '--- Move on!'
%except ZeroDivisionError:
%  print '0/0: 0.00\%'
%
%\end{python}

\end{document}
